# Copy to .env and fill in real values. .env is gitignored; this template is tracked.

# BlenderKit API key (blenderkit.com -> profile -> API key). Used by Tools/blenderkit_fetch.py
# to search/download library assets (interior/exterior kitbash props, materials) headlessly.
BLENDERKIT_API_KEY=
